# Example server with Nucleoid

## Run in debug mode

```shell
dart run --enable-asserts bin/main.dart
```
## Build release app

```shell
dart pub run nucleoid build
```

## Run building app

```shell
(cd build && ./app.server_template.exe)
```